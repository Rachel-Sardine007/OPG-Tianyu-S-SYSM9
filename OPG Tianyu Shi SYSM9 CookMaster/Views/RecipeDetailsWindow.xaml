<Window x:Class="OPG_Tianyu_Shi_SYSM9_CookMaster.Views.RecipeDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPG_Tianyu_Shi_SYSM9_CookMaster.Views" 
        xmlns:viewmodels="clr-namespace:OPG_Tianyu_Shi_SYSM9_CookMaster.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:RecipeDetailsViewModel}"
        mc:Ignorable="d"
        Title="RecipeDetailsWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Margin="20">
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Margin" Value="0,5,0,5"/>
                </Style>
                <Style TargetType="TextBox">
                    <Setter Property="IsEnabled" Value="False"/>
                </Style>
            </StackPanel.Resources>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Recipe Details" FontSize="25" FontWeight="Medium" Margin="0,0,50,0"/>
                <Image Source="C:\Mac\Home\Documents\Csharp101\Inluppg\OPG Tianyu Shi SYSM9 CookMaster\OPG Tianyu Shi SYSM9 CookMaster\Views\Pages\SplashImage.png"
                Width="110"/>
            </StackPanel>
            <TextBlock Text="{Binding Error}" Foreground="Red"/>
            <TextBlock Text="Title"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                     x:Name="Txb_Title"/>
            <TextBlock Text="Ingredients"/>
            <TextBox Text="{Binding Ingredients, UpdateSourceTrigger=PropertyChanged}"
                     x:Name="Txb_Ing"/>
            <TextBlock Text="Instruction"/>
            <TextBox Text="{Binding Instructions, UpdateSourceTrigger=PropertyChanged}"
                     x:Name="Txb_Ins" Height="50"/>
            <TextBlock Text="Category"/>
            <TextBox Text="{Binding Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     x:Name="Txb_Category"/>
            <TextBlock Text="Date"/>
            <DatePicker x:Name="Txb_Date" Margin="0,5,0,5" 
                        Text="{Binding SelectedDate,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <StackPanel Grid.Column="2" Grid.ColumnSpan="2" Margin="10" VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="100"/>
                </Style>
            </StackPanel.Resources>
            <Button x:Name="Btn_Save" Content="Save" Margin="0,5,0,5" Command="{Binding SaveCommand}"
                    Visibility="Hidden"/>
            <Button x:Name="Btn_Edit" Content="Edit" Margin="0,5,0,5" Click="Button_Click"/>
            <Button Content="Copy" Margin="0,5,0,5" Command="{Binding CopyCommand}"/>
            <Button Content="Return" Margin="0,5,0,5" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
